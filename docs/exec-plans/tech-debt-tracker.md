# Tech debt tracker

Bewuste shortcuts die later opgelost moeten worden. Voeg een regel toe zodra je er willens en wetens een neemt — niet achteraf reconstrueren.

| Datum | Domein | Shortcut | Waarom | Op te lossen wanneer |
|---|---|---|---|---|
| 2026-09-01 | providers (card-catalog) | `PokemonTcgProvider.getPrice`/`MagicProvider.getPrice` gooien een expliciete "niet geïmplementeerd"-error; geen retries/timeouts rond `search()`/`getById()` ondanks de bekende reliability-issues van beide API's (Pokémon TCG API met name — meermaals 500/502 gezien tijdens ontwikkeling). | 01-card-catalog.md sluit prijs bewust uit (zie het exec-plan "Doel"); retries/timeouts zijn geen blokkade voor zoeken/browsen. | Prijs: zodra `valuation` gebouwd wordt. Retries: zodra de flakiness in de praktijk een probleem blijkt voor gebruikers. |
| 2026-09-01 | providers (card-catalog) | `Card.id` is een samengestelde placeholder (`tcg:externalId`) i.p.v. een echte uuid; `variant` staat altijd op `'normal'` (geen foil/reverseFoil-detectie via `tcgplayer.prices`/`prices.*_foil`). | Geen persistentielaag/variant-logica nog aanwezig in card-catalog. | Zodra card-catalog/repo daadwerkelijk kaarten persisteert en varianten toont. |
| 2026-09-01 | provider-fixtures | Geen reverse-foil-scenario voor de Pokémon TCG API (wel: normale kaart + card-by-id). Scryfall heeft wel een dubbelzijdige-kaart- en een nul-resultaten-scenario. | Variant-mapping is nog niet geïmplementeerd (zie regel hierboven). | Zodra card-catalog varianten daadwerkelijk normaliseert. |
| 2026-09-01 | providers (Scryfall) | `MagicProvider` zet wel een eigen `User-Agent`-header, maar die wordt in een échte browser genegeerd (forbidden header, Fetch-spec) — de browser stuurt dan zijn eigen UA, wat toevallig ook voldoet aan Scryfall's regel (die alleen generieke HTTP-library-defaults zoals Node's fetch-UA afwijst met 400 `generic_user_agent`). Geen garantie dat dit voor altijd zo blijft. | Ontdekt doordat de smoke test (Node) 400'de op de generieke UA totdat de header werd toegevoegd. Server-side wordt de header wel echt gebruikt. | Heroverwegen als Scryfall ooit ook echte browser-UA's gaat afwijzen, of als er toch een server-side proxy komt (zie SECURITY.md over Cloud Functions). |
| 2026-09-01 | Firestore rules | Veldnamen in `firestore.rules` (`ownerId`, `portfolioId`) zijn een aanname vooruitlopend op de echte schema's van `collection`/`trade-analyzer`. | Rules moesten van vóór de eerste domeindata af bestaan (SECURITY.md), maar die domeinen hebben nog geen Types. | Zodra collection/trade-analyzer hun Types definiëren — rules + rules-tests meebewegen. |
